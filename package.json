{
  "name": "sebolig",
  "private": true,
  "version": "1.0.0",
  "description": "SeBolig monorepo",
  "scripts": {
    "build": "npm run build:client",
    "build:client": "npm run build --workspace=client",
    "start": "npm run start --workspace=server",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:client": "npm run dev --workspace=client",
    "dev:client:host": "npm run dev:host --workspace=client",
    "dev:server": "npm run dev --workspace=server",
    "test:sort": "npm run test:sort --workspace=server",
    "clean:client": "npm run clean --workspace=client",
    "clean:server": "npm run clean --workspace=server",
    "clean": "npm run clean:client && npm run clean:server"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
