OPENAI_API_KEY=******** # OpenAI API key
COOKIE_SECRET=******** # Secret used for signing cookies
IMAGE_BASE_URL=https://findbolig.nu # Origin for listing images; used to pre-warm the image proxy cache
